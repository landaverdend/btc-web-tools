export default class Tx {
  // version: number;
  // inputs: TxInput[];
  // outputs: TxOutput[];
  // locktime: number;

  // constructor(version: number, inputs: TxInput[], outputs: TxOutput[], locktime: number) {
  //   this.version = version;
  //   this.inputs = inputs;
  //   this.outputs = outputs;
  //   this.locktime = locktime;
  // }
}


